# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             127.0.0.1/32            md5
# IPv6 local connections:
host    all             all             ::1/128                 md5
# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5

# Allow connections from Docker network (with and without SSL)
host    all             all             172.16.0.0/12           md5
hostnossl all           all             172.16.0.0/12           md5

# Allow connections from any IP (for development/remote access)
# CAUTION: Only use this if you have proper firewall rules
host    all             all             0.0.0.0/0               md5
hostnossl all           all             0.0.0.0/0               md5